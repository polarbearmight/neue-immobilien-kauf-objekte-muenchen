# Memory Log — 2026-03-06

## Durable project progress (neue-immobilien-kauf-objekte-muenchen)

Implemented and merged major architecture/features in ordered delivery blocks with tests passing:

1. **Stop-early + resilient orchestration**
   - Branch: `feat/stop-early-incremental-orchestrator`
   - Commit: `43ec850`
   - Merge to main: `437a5a3`
   - Added stop-early controls + parallel/resilient run_targets + tests.

2. **Scan Sources button + background scan API/status**
   - Branch: `feat/scan-sources-button`
   - Commit: `a929503`
   - Merge to main: `137b6d2`
   - Added `/api/scan/run`, `/api/scan/status`, single-scan lock, progress tracking, frontend button states/progress.

3. **Expanded Listing Detail Drawer**
   - Branch: `feat/detail-drawer-expanded`
   - Commit: `8967e2e`
   - Merge to main: `3174943`
   - Added `/api/listings/{id}/detail` with source reliability, cluster members, canonical hints, and price history sections in UI.

4. **Investment Score + Yield Estimation**
   - Branch: `feat/investment-score-yield`
   - Commit: `b69d59f`
   - Merge to main: `24f2099`
   - Added investment model fields, migration/indexes, `app/investment.py`, recompute integration, drawer display, tests.

5. **Duplicate Detection across sources (fuller)**
   - Branch: `feat/duplicate-detection-cross-source`
   - Commit: `f4cde99`
   - Merge to main: `e0d3371`
   - Improved dedup logic (address/title/price/size/district/postal heuristics), canonical cluster payload in API/UI, tests.

6. **Deal Highlighting (visual prioritization)**
   - Branch: `feat/deal-highlighting-visual-priority`
   - Commit: `ecf2ee7`
   - Merge to main: `5679e2d`
   - Added reusable highlight badge/tone logic and stronger visual hierarchy in listing table + deal radar.

7. **Off-Market Detector + /off-market page**
   - Branch: `feat/off-market-detector-page`
   - Commit: `f1c598f`
   - Merge to main: `fcb5ac8`
   - Added off-market scoring pipeline/fields/indexes, `/api/off-market`, off-market page, drawer section, tests.

## Validation snapshot
- Backend tests increased and passed through all steps (ended at **40 passed**).
- Frontend lint consistently clean except one pre-existing TanStack compatibility warning in `listing-table.tsx`.

## Process preference reinforced
- User requested strict workflow: branch-per-feature, tests, push, direct merge to `main`, and report hashes.
- In topic context, concise progress responses are preferred; user sometimes requested silence until feature completion.

## Nachtrag (hinzugefügt am 2026-03-07 00:51 UTC)
- Nutzerwunsch notiert: „Bitte zu gestern hinzufügen“.

## Fitness-/Ernährungsnachtrag aus Screenshot (für „Gestern“)
- Frühstück: **2.294 kcal** (98 g Fett, 88 g KH, 256 g Protein)
- Mittagessen: **751 kcal** (12 g Fett, 102 g KH, 51 g Protein)
- Bisher sichtbar gesamt (Frühstück + Mittag): **3.045 kcal**
  - Makros gesamt: **110 g Fett, 190 g KH, 307 g Protein**
- Aktivitäten: **-318 kcal** (laut Eintrag „Aktivitäten“)
